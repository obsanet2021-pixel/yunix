# Secrets required by edge functions in the target Supabase project.
# Re-create each one in Project Settings → Edge Functions → Secrets.
# Values are NOT exported (sensitive). You must obtain them from each provider.

LOVABLE_API_KEY            # Lovable AI Gateway (won't work outside Lovable — get your own OpenAI/Gemini key and refactor functions)
RESEND_API_KEY             # Resend.com — for email (https://resend.com/api-keys)
FOREX_API_KEY              # Forex data provider used by forex-data function
ENCRYPTION_KEY             # 32-byte base64 key for encrypt-password / decrypt-password (generate new + re-encrypt data)
TELEGRAM_BOT_TOKEN         # Main Telegram bot (BotFather)
TELEGRAM_SUPPORT_BOT_TOKEN # Support bot
CEO_BOT_TOKEN              # CEO notifications bot
DELIVERY_BOT_TOKEN         # Delivery agent bot
EXTERNAL_SUPABASE_URL      # If you keep cross-project sync, set to a remote Supabase URL
EXTERNAL_SUPABASE_SERVICE_KEY  # Service key for that external project

# Supabase auto-injects these — DO NOT set manually:
# SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_DB_URL, SUPABASE_PUBLISHABLE_KEY
